'use client'
import React from 'react'
import PredictForm from '../components/PredictForm'

const PredictContainer = () => {
  return <PredictForm></PredictForm>
}

export default React.memo(PredictContainer)
